# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database
sqlalchemy==2.0.35
aiosqlite==0.20.0          # SQLite async driver (swap for asyncpg when moving to Postgres)
alembic==1.13.3            # Schema migrations — production-grade, Postgres-compatible

# Auth
python-jose[cryptography]==3.3.0
bcrypt==4.2.0
pydantic-settings==2.5.2
pydantic[email]==2.9.2

# HTTP client (market data + research APIs)
httpx==0.27.2

# AI
openai==1.51.0

# Research providers (sponsor integrations)
tavily-python==0.5.0       # Tavily Search API — Tier 3 verification layer only (see app/documents/)

# Document Intelligence (Phase 2) — primary-source ingestion, not web search
beautifulsoup4==4.12.3     # HTML section parsing for SEC filings + generic IR pages
pypdf==5.0.1               # PDF text + page extraction (investor decks, shareholder letters)
tiktoken==0.8.0            # accurate token counting for budgeted chunking

# Utils
python-dotenv==1.0.1
