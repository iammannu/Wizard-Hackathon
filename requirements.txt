# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database
sqlalchemy==2.0.35
aiosqlite==0.20.0          # SQLite async driver (swap for asyncpg when moving to Postgres)
alembic==1.13.3            # Schema migrations — production-grade, Postgres-compatible

# Auth
python-jose[cryptography]==3.3.0
bcrypt==4.2.0
pydantic-settings==2.5.2
pydantic[email]==2.9.2

# HTTP client (market data + research APIs)
httpx==0.27.2

# AI
openai==1.51.0

# Research providers (sponsor integrations)
tavily-python==0.5.0       # Tavily Search API — Tier 3 verification layer only (see app/documents/)

# Document Intelligence (Phase 2) — primary-source ingestion, not web search
beautifulsoup4==4.12.3     # HTML section parsing for SEC filings + generic IR pages
pypdf==5.0.1               # PDF text + page extraction (investor decks, shareholder letters)
tiktoken==0.8.0            # accurate token counting for budgeted chunking

# Semantic Retrieval Engine (Milestone 1)
numpy==1.26.4              # brute-force cosine similarity for SQLiteVectorStore (the active default)
rank-bm25==0.2.2           # BM25 half of hybrid search

# Optional — install only if you select the corresponding provider/store in
# .env (embedding_provider / vector_store_provider). Lazy-imported, so the
# app runs fine without these until you opt in.
# faiss-cpu==1.8.0              # vector_store_provider=faiss
# chromadb==0.5.5               # vector_store_provider=chroma
# pinecone-client==5.0.1        # vector_store_provider=pinecone
# sentence-transformers==3.1.1  # embedding_provider=local (also pulls in torch)

# Utils
python-dotenv==1.0.1
